

// Consejos a la hora de comparar booleans

const autenticado = true;

if(autenticado) {
    console.log('Sí puedes ver netflix');
} else {
    console.log('No, no puedes ver Netflix');
}


// Operador Ternario

console.log( autenticado ? 'Sí esta autenticado' : 'No esta autenticado');