const diaHoy = new Date();

let valor;

valor = diaHoy;

// Trae el año actual
valor = diaHoy.getFullYear();

// Trae el mes actual
valor = diaHoy.getMonth();

// Trae los minutos
valor = diaHoy.getMinutes();